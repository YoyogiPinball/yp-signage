> 最終更新: 2026-08-24（Mon）09:12

# 変更履歴

バージョン番号は付けていません。日付で区切ります。新しいものが上です。

すでに動かしている環境を更新するときは、まず「更新するときの注意」を読んでください。
`.env` の書き換えが必要かどうかはそこに書いてあります。

## 2026-08-24

### 追加

- **次につけるまで消す**：自動予定では点灯せず、WebリモコンまたはSSHコマンドで明示的に点灯するまで消灯を維持します。
- **SSH復帰コマンド**：`bash ~/run/signage-display-on.sh` で期限なし消灯を解除できます。

### 更新するときの注意

**`.env` と `timer.env` の書き換えは要りません。**
`deploy.sh signage` を実行すると、SSH復帰スクリプトも `~/run/` へ配布されます。

## 2026-08-23

曜日ごとの予定で外部モニターだけを点灯・消灯する画面タイマーと、ブラウザから操作するリモコンを追加しました。
MagicMirror² は止めないため、モニターを点けると元の画面がすぐに表示されます。

### 追加

- **画面タイマー** — 平日は 07:00〜10:00 / 12:00〜13:00 / 18:00〜25:00、土日は 07:00〜25:00 を
  既定の点灯予定として動きます。予定は曜日ごとに編集でき、`25:00` は翌日 01:00 を表します。
- **Web リモコン** — 画面の点灯、次の自動切り替えまたは翌朝 7 時までの消灯、自動運転への復帰、
  スライドショーの一時停止・再開・前後送り、点灯予定の編集ができます。既定では `http://127.0.0.1:8081` で待ち受け、
  外から開くときは Tailscale Serve を使います。
- **`signage-timer.service`** — `deploy.sh` がユーザーサービスと実行ファイルを表示機へ配ります。
  有効化済みの環境では更新時に再起動し、未設定の環境では勝手に常駐させません。

### 更新するときの注意

**既存の環境で `deploy.sh` を実行しても、`systemctl --user enable --now signage-timer` を実行するまで何も起きません。**
画面タイマーを使う表示機で、このコマンドを 1 回だけ実行してください。

**`SIGNAGE_TIMER_USER` を設定しないまま Tailscale Serve で公開すると、Tailnet に入っている人なら誰でも操作できます。**
表示機の `~/.config/yp-signage/timer.env` に自分の Tailscale ログイン名を書き、サービスを再起動してください。

**`magicmirror/.env` の書き換えは要りません。** 画面タイマーの設定は別の `timer.env` に置きますが、こちらも無くても既定値で動きます。

## 2026-08-22

時計が背景写真に溶けて読めなくなる問題に対応しました。あわせて、その濃さを画面を見ながら
調整できるようにしています。

### 追加

- **時計の背景プレート** — 時計モジュールにだけ、常時、半透明の暗い板を敷くようにしました。
  上バー全体の板（`mm-ctl.sh topbar` で出し入れするもの）とは別で、こちらは常に出ています。
  板は時計の中身の大きさに合わせ、文字まわりの空きが4辺とも 27px になるようにしてあります。
- **`mm-ctl.sh plate {0-100|reset}`** — その板の濃さ(%)を、MagicMirror² を再起動せずに変えます。
  写真を眺めながら濃さを詰めるためのもので、再起動すると既定へ戻ります。
  気に入った値が決まったら `magicmirror/css/custom.css` の `--clock-plate-alpha` に書き戻してください
  （書き戻す値は `plate` 実行時のメッセージに出ます）。

### 変更

- **時計の文字を大きくしました。** 時刻 `4.14rem` → `4.7rem`、日付 `1.6rem` → `1.8rem`。
- **板が上バーの高さいっぱいに伸びるのをやめました。** 中身の高さに合わせて、上バーの中で縦中央に置きます。
  これまでは板の高さの半分以上が空きでした（上バーの高さを決めているのは月カレンダーで、時計はそこまで背が高くないため）。

### 更新するときの注意

**`.env` の書き換えは要りません。** 板の濃さは `magicmirror/css/custom.css` の `:root` にある
`--clock-plate-alpha` で決まります（既定 `0.27`）。板が要らない場合は `0` にしてください。

板の内側の余白（`padding: 23px 0 5px 24px`）は、4辺の見た目の空きが 27px でそろうように実測で決めた値です。
数字が非対称なのは、文字の外側にもともと付いている空きが辺ごとに違うためです。
**日付の書式（`SIGNAGE_*` ではなく `config.js` の `dateFormat`）を変えると右の空きが変わるので、そのときは測り直してください。**

背景写真が明るいと、板の無い月カレンダーと天気は読みにくいままです。これは時計だけに板を敷く
設計を選んだ結果で、不具合ではありません。気になる場合は上バー全体の板を使ってください。

## 2026-08-12

背景スライドショーの再生方法を選べるようにしました。これまでは「シャッフルするかしないか」だけで、
最後の画像まで来たら必ず先頭へ戻る動きに固定されていました。

### 追加

- **表示順の設定 `SIGNAGE_ORDER_MODE`** — `sequential`（ファイル名の自然順。`2.jpg` が `10.jpg` より先）か
  `shuffle`（順番を混ぜる）を選べます。
- **末尾の動きの設定 `SIGNAGE_REPEAT_MODE`** — `all`（先頭へ戻る）／`none`（そこで自動送りを終える）／
  `one`（自動送りでは画像を変えず 1 枚を出しっぱなしにする）から選べます。
- **画面から再生モードを切り替えられるようになりました。** 右クリックメニューに「並び順」と「くり返し」が並び、
  押すたびに次の値へ切り替わります。手元からは `mm-ctl.sh order shuffle` / `mm-ctl.sh repeat none` でも変えられます。
  この変更は一時的なもので、MagicMirror² を起動し直すと `.env` の値に戻ります。
- **`mm-ctl.sh restart`** — 最初の画像から再生し直します。
- **状態バッジ** — 画面右下に、止まっている理由を出すようにしました。「❙❙ 一時停止中」（自分で止めた）、
  「■ 再生終了」（`none` で最後まで再生し終えた）、「⚠ 画像を読み込めません」（並びの画像がすべて壊れている）の 3 種類です。
- **テスト** — 再生の並び・現在位置・状態遷移を `magicmirror/modules/yp-slideshow/playback.js` に切り出し、
  `node --test tests/` で検証できるようにしました。`npm install` は要りません（Node 20 以降の標準機能だけを使います）。

### 変更

- ファイル名の並び順を**自然順**にしました。これまでは文字コード順で、`10.jpg` が `2.jpg` より前に来ていました。
- 一時停止中のバッジの見た目は変わっていませんが、内部のクラス名を `.yp-paused` から `.yp-badge` に変えました。
  ユーザー CSS でこのクラスを上書きしていた場合は追随が必要です。

### 修正

- 一時停止中に壊れた画像へ手動で送ると、自動再生が勝手に再開していました。
- 画像フォルダが空になると内部の位置が数値でない値になり、画像を戻しても表示が復帰しませんでした。
- 読み込めない画像の通知が切り替えのフェード中に届くと、画面が真っ黒のまま止まることがありました。
  自動送りが続いていれば次の切り替えで直りますが、`SIGNAGE_REPEAT_MODE=none` の末尾では戻りませんでした。

### 更新するときの注意

**`.env` を書き換えなくても、これまでどおり動きます。** 古い設定 `SIGNAGE_SHUFFLE` は残してあり、
上の新しい 2 項目を**どちらも書いていないとき**だけ読みます。`true` はシャッフル＋先頭へ戻る、
`false` はファイル名順＋先頭へ戻る、として解釈します。

そのうえで 2 点だけ気をつけてください。

- **`SIGNAGE_SHUFFLE=false` で使っていた場合、画像の並ぶ順が変わります。** 文字コード順から自然順になるためです。
  動かなくなるわけではありませんが、見える順番は変わります。
- **新旧を混ぜると、古い設定は無視されます。** 新しい 2 項目のうち片方だけを書くと `SIGNAGE_SHUFFLE` は読まれず、
  起動時に警告が 1 行出ます。新旧が半分ずつ効いている状態を作らないためです。移すときは 2 項目とも書いてください。

書き換えるなら、`SIGNAGE_SHUFFLE` の行を消して次の 2 行を入れます。これまでと同じ動きになります。

```bash
SIGNAGE_ORDER_MODE=shuffle
SIGNAGE_REPEAT_MODE=all
```

`SIGNAGE_SHUFFLE` はいずれ読まなくなる予定です。

> **Note:** 配布は `./deploy.sh` です。`magicmirror/modules/yp-slideshow/playback.js` が増えているので、
> 手でコピーしている場合はこのファイルも忘れずに置いてください。無いと画面が出ません。
