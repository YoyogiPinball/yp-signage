# x13 — ThinkPad X13 常時稼働サイネージ/サーバー

WSL側ソース（正本）。ドキュメントは Obsidian `40-Projects/X13/`。

- `scripts/` … X13へ配布するスクリプトの正本
- `deploy.sh` … scripts/ を X13(`ssh x13`)へ配布
- `sway/` … Sway 設定（移行後）

X13: 固定IP 192.168.x.x / user youruser / `ssh x13` 鍵認証済み。
